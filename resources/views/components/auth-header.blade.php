@props(['title', 'description'])

<div class="mb-8">
    <p class="mb-3 text-xs font-bold uppercase tracking-[0.24em] text-teal-700">Secure workspace</p>
    <h1 class="font-display text-3xl leading-tight text-[#173b37] sm:text-4xl">{{ $title }}</h1>
    <p class="mt-3 leading-6 text-stone-600">{{ $description }}</p>
</div>
